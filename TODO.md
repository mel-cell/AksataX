# TODO

- [ ] Inspect `app/page.tsx` and fix any syntax/duplicate exports causing the error.
- [ ] Ensure home page renders categories AND provides navigation to postingan.
- [ ] Run `npm run lint` and/or `npx tsc --noEmit` to confirm fix.
